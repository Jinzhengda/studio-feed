# Studio Feed 测试说明

## 自动执行的安全测试

以下测试不会创建账号或写入业务数据：

1. 公开首页、登录、注册、找回密码、关于、联系页面可访问。
2. 登录页密码显示/隐藏和注册链接正确。
3. 注册密码少于 8 位时浏览器阻止提交。
4. 未登录访问个人设置、工作室管理、作品管理和欢迎页时跳转登录。
5. 未登录调用工作室、作品和手动刷新接口时返回 401。
6. Cron 接口缺少密钥或密钥错误时返回 401。
7. 媒体代理拒绝缺失地址、非 HTTP 协议和非白名单域名。
8. 工作室 URL 校验拒绝 localhost、内网 IP、链路本地地址、云元数据地址和内部域名。
9. 登录、注册和密码恢复的错误/成功提示不会泄露账号是否存在。

## 需要两个专用测试账号的测试

在本机环境中配置以下变量后，Playwright 会自动执行：

```text
E2E_USER_A_EMAIL=
E2E_USER_A_PASSWORD=
E2E_USER_B_EMAIL=
E2E_USER_B_PASSWORD=
```

覆盖内容：

1. 两个账号分别登录。
2. 账号 A 创建工作室后，账号 B 的列表中不可见。
3. 账号 B 无法修改或真正删除账号 A 的工作室。
4. 测试完成后自动删除账号 A 创建的测试工作室。
5. 登录状态下仍拒绝 SSRF 内网地址。

可选开关：

- `E2E_USER_B_EXPECT_EMPTY=1`：确认账号 B 是空账号，并测试新用户空状态。
- `E2E_ENABLE_REFRESH_TEST=1`：执行两次手动刷新并验证第二次返回 429。此项会写入刷新冷却状态。

## 默认不自动执行

以下项目需要独立测试环境、邮箱收件能力或更多外部资源，默认保留为人工/上线前验收项：

1. 真实注册邮件、邮箱验证回调和真实密码重置邮件。
2. 第 51 家工作室上限。自动创建 51 条记录会污染共享数据库。
3. 真实抓取第三方网站、重定向到内网和部分站点失败隔离。
4. Cron 使用正确密钥后的真实批量刷新。
5. 头像上传及两个账号之间的 Storage 隔离。
6. Supabase RLS 的 SQL 级直接越权调用。

这些测试应在独立 Supabase 测试项目中执行，不应使用生产数据库。

## 运行方法

```bash
npm run test:e2e
```

成功标志：终端最后显示通过数量，且没有 `failed`。

查看图形化报告：

```bash
npm run test:e2e:report
```

交互式调试：

```bash
npm run test:e2e:ui
```
