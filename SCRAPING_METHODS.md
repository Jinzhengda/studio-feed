# 抓取方法说明

本文档说明了系统支持的各种抓取方法，帮助你在添加新工作室时选择合适的抓取地址。

## 方法 1：直接使用作品列表页面 URL

**适用场景**：网站有明确的作品列表页面，且使用服务端渲染（SSR）

**示例**：
- UDL: `https://u-d-l.com/`
- NDC: `https://www.ndc.co.jp/zh/`
- Pentagram: `https://www.pentagram.com/work`

**如何判断**：
1. 在浏览器中打开页面
2. 右键 → 查看网页源代码
3. 如果能在源代码中看到作品标题和链接，说明可以用这个方法

**填写方式**：
- RSS / 抓取地址：填写作品列表页面的完整 URL

---

## 方法 2：使用 RSS Feed

**适用场景**：网站提供 RSS 订阅源

**示例**：
- 某些博客类网站：`https://example.com/feed.xml`
- 某些设计网站：`https://example.com/rss`

**如何查找 RSS**：
1. 查看网站底部是否有 RSS 图标
2. 在浏览器中查看源代码，搜索 `rss` 或 `feed`
3. 尝试访问 `/feed`、`/rss`、`/feed.xml` 等常见路径

**填写方式**：
- RSS / 抓取地址：填写 RSS feed 的完整 URL

---

## 方法 3：使用 API 端点（高级）

**适用场景**：网站使用前端框架（React/Vue），通过 API 加载数据

**如何查找 API**：
1. 打开浏览器开发者工具（F12）
2. 切换到 Network 标签
3. 刷新页面，查看 XHR/Fetch 请求
4. 找到返回作品数据的 API 端点

**示例**：
- 某些网站可能有：`https://api.example.com/works`
- 或者：`https://example.com/api/projects.json`

**填写方式**：
- RSS / 抓取地址：填写 API 端点的完整 URL

---

## 方法 4：上传默认封面（兜底方案）

**适用场景**：
- 网站使用 JavaScript 动态渲染，无法直接抓取
- 网站结构复杂，抓取效果不理想
- 临时解决方案

**操作步骤**：
1. 在工作室管理页面，点击"编辑"
2. 在"封面图 URL"下方，点击"上传"按钮
3. 选择一张代表该工作室的图片
4. 保存

**效果**：
- 当抓取不到作品图片时，会显示这张默认封面
- 至少保证页面有图片显示

---

## 当前支持的网站类型

### 已优化支持（有专门抓取逻辑）：
1. **UDL** (u-d-l.com) - 使用 bg-set 属性
2. **NDC** (ndc.co.jp) - 日文网站
3. **portorocha** (portorocha.com) - 葡萄牙语网站

### 通用支持（自动识别）：
- 包含 `/work`、`/project`、`/portfolio`、`/case` 路径的网站
- 使用常见 CSS 类名的网站（`.work`、`.project` 等）
- 使用 `data-project`、`data-work` 属性的网站

---

## 常见问题

### Q: 为什么有些工作室抓取不到数据？

**可能原因**：
1. **JavaScript 渲染**：网站使用前端框架，内容通过 JS 动态加载
2. **需要登录**：网站需要登录才能查看作品
3. **反爬虫机制**：网站检测到爬虫请求并拒绝访问
4. **URL 错误**：填写的 URL 不是作品列表页面

**解决方法**：
- 尝试不同的 URL（如 `/work`、`/projects`、`/portfolio`）
- 查找是否有 RSS feed
- 上传默认封面作为兜底方案
- 联系开发者添加专门的抓取支持

### Q: 为什么抓取到的图片是占位图？

**可能原因**：
1. 网站使用懒加载（lazy loading）
2. 图片 URL 在 JavaScript 中动态生成
3. 图片需要特殊的请求头才能访问

**解决方法**：
- 上传工作室默认封面
- 联系开发者优化抓取逻辑

### Q: 如何提高抓取成功率？

**建议**：
1. 填写作品列表页面的 URL，而不是首页
2. 确保 URL 可以直接访问（不需要登录）
3. 优先选择有 RSS feed 的网站
4. 为每个工作室上传默认封面

---

## 需要帮助？

如果某个工作室无法抓取，可以：
1. 检查 URL 是否正确
2. 尝试不同的页面 URL
3. 上传默认封面作为临时方案
4. 记录问题，等待系统更新支持
